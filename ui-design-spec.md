# UI 设计稿规格文档 - 个人智能写作与分发工作台

> 本文档详细描述每个页面、组件和交互状态，可直接输入至 Figma AI / v0 / Claude Artifacts 等 AI 设计工具生成视觉稿。

---

## 一、设计系统总览

### 1.1 设计风格

- **风格定位**: 现代暖白风 (Modern Warm White) — 类似 Notion / 飞书，扁平干净、友好亲切
- **设计语言**: 内容优先、零装饰噪音、无阴影纯色块、温暖明亮的创作氛围
- **圆角系统**: 统一 `8px` (卡片/按钮), `6px` (输入框), `12px` (弹窗/抽屉)
- **阴影系统**: 主题以纯色块区分层次，不依赖阴影；悬浮菜单使用 `0 4px 24px rgba(0,0,0,0.06)` 轻阴影

### 1.2 色彩系统 (Light Mode 默认主题)

| Token 名称 | 色值 | 用途 |
|------------|------|------|
| `bg-deepest` | `#FAFAFA` | 全局页面底色 |
| `bg-surface` | `#FFFFFF` | 卡片/面板/编辑器/侧边栏背景 |
| `bg-elevated` | `#FFFFFF` | 悬浮菜单/下拉/弹窗 (加轻阴影) |
| `bg-hover` | `#F5F5F5` | 行级/列表项 hover 态 |
| `bg-pressed` | `#EBEBEB` | 按下态 |
| `border-default` | `#E5E5E5` | 分割线、边框 |
| `border-subtle` | `#F0F0F0` | 弱分割线 |
| `text-primary` | `#1A1A1A` | 主标题/正文 |
| `text-secondary` | `#6B7280` | 次要信息/时间戳/文件路径 |
| `text-muted` | `#9CA3AF` | 占位符/禁用态 |
| `accent-primary` | `#0D9488` | 通用主操作按钮 (蓝绿) |
| `accent-primary-hover` | `#0F766E` | 主按钮 hover 态 |
| `accent-primary-light` | `#F0FDFA` | 主色浅底 (标签/选中背景) |
| `accent-cta` | `#EA580C` | 强调 CTA 按钮 (暖橙) |
| `accent-wechat` | `#07C160` | 微信公众号相关操作高亮 |
| `accent-xhs` | `#FF2442` | 小红书相关操作高亮 |
| `accent-ai` | `#8B5CF6` | AI 功能相关操作高亮 (紫色) |
| `accent-ai-light` | `#F5F3FF` | AI 功能浅底 |
| `status-success` | `#059669` | 日志成功状态 |
| `status-error` | `#DC2626` | 日志失败/错误状态 |
| `status-warning` | `#D97706` | 日志警告状态 |
| `status-info` | `#0284C7` | 日志信息状态 |

### 1.3 Dark Mode 主题 (可选切换)

| Token 名称 | 色值 | 用途 |
|------------|------|------|
| `bg-deepest` | `#111111` | 全局页面底色 |
| `bg-surface` | `#1A1A1A` | 卡片/面板背景 |
| `bg-elevated` | `#242424` | 悬浮菜单/弹窗背景 |
| `bg-hover` | `#2A2A2A` | hover 态 |
| `border-default` | `#333333` | 边框 |
| `text-primary` | `#E8E8E8` | 主文字 |
| `text-secondary` | `#9CA3AF` | 次要文字 |
| `text-muted` | `#6B7280` | 占位符 |
| `accent-primary` | `#14B8A6` | 主操作 (蓝绿, 亮度提升以适配暗底) |
| `accent-ai` | `#A78BFA` | AI 功能 (紫色, 亮度提升) |

> AI 设计工具提示：以 Light Mode 为默认生成，Dark Mode 仅替换色值 token，布局/组件结构不变。

### 1.4 字体系统

| 层级 | 字体 | 字号 | 字重 | 行高 | 用途 |
|------|------|------|------|------|------|
| Display | Plus Jakarta Sans | 28px | 700 | 1.3 | 页面大标题 |
| Heading 1 | Plus Jakarta Sans | 22px | 600 | 1.4 | 区域标题 |
| Heading 2 | Plus Jakarta Sans | 18px | 600 | 1.4 | 子区域标题 |
| Body | Plus Jakarta Sans | 15px | 400 | 1.6 | 正文/描述 |
| Caption | Plus Jakarta Sans | 13px | 400 | 1.5 | 辅助说明/时间 |
| Code | JetBrains Mono | 14px | 400 | 1.6 | 编辑器内/代码片段 |
| Mono | JetBrains Mono | 12px | 400 | 1.5 | 日志输出窗/标签 |

> **Google Fonts 链接**: `Plus Jakarta Sans` (wght 400;500;600;700) + `JetBrains Mono` (wght 400;500)

### 1.5 间距系统

采用 4px 基础网格：`4 / 8 / 12 / 16 / 24 / 32 / 48 / 64`

### 1.6 图标系统

- **图标库**: Lucide Icons (线条风格, stroke-width: 1.5)
- **尺寸规范**: 16px (行内), 20px (按钮), 24px (导航)
- **禁止**: 使用 Emoji 作为功能图标

---

## 二、页面清单

| 编号 | 页面名称 | 路由 | 说明 |
|------|---------|------|------|
| P1 | PC 主工作台 | `/` | 核心双栏编辑界面 |
| P2 | 移动端编辑页 | `/` (移动端) | 全屏编辑器 |
| P3 | 移动端预览页 | `/preview` | 独立预览页面 |
| P4 | 设置页 | `/settings` | API Key、账号、偏好配置 |
| P5 | 文件管理侧边栏 | 内嵌组件 | 文档树/文件列表 |

---

## 三、页面详细设计

---

### P1: PC 主工作台 (`/`)

#### 3.1 整体布局

```
┌─────────────────────────────────────────────────────────────────────┐
│  [GLOBAL TOP BAR]  高度: 48px, bg: #FFFFFF, border-bottom: #E5E5E5 │
├──────────┬──────────────────────────────────────────────────────────┤
│          │                                                          │
│  [FILE   │  [LEFT PANEL]          │  [RIGHT PANEL]                 │
│  SIDEBAR]│  编辑区                 │  预览与分发区                  │
│  宽度:   │  可拖拽调宽             │  可拖拽调宽                    │
│  240px   │  min: 400px            │  min: 360px                    │
│  可折叠  │                        │                                │
│          │                        │                                │
│          │                        │                                │
├──────────┴──────────────────────────────────────────────────────────┤
│  [STATUS BAR]  高度: 28px, bg: #FAFAFA (可选，显示字数/保存状态)    │
└─────────────────────────────────────────────────────────────────────┘
```

#### 3.2 全局顶栏 (Global Top Bar)

**尺寸**: 全宽 x 48px
**背景**: `bg-surface` (#FFFFFF), 底部 1px `border-default` (#E5E5E5)

从左到右元素排列：

| 位置 | 元素 | 规格 | 说明 |
|------|------|------|------|
| 最左 | 文件侧边栏折叠按钮 | 24px Lucide 图标 (`PanelLeft`) | 点击展开/收起左侧文件栏 |
| 左侧 8px | Logo 区域 | 文字 Logo "AI Writer" 或 SVG 图标, 字重 600, 16px | 纯文字，无图片 |
| 左侧 16px | 当前文件名 | Inter 14px, text-primary, 显示当前打开的 .md 文件名 | 可点击展开文件路径面包屑 |
| 居中 | AI Agent 状态指示 | 圆点 8px + 文字 "DeepSeek-V3 在线" 或 "Claude Sonnet 在线" | 圆点颜色 = accent-ai (#8B5CF6) |
| 右侧 16px | 模型切换下拉框 | 宽度 180px, 高度 32px, bg-elevated, 圆角 4px | 下拉选项: DeepSeek-V3 / Claude Sonnet / Qwen-Max |
| 右侧 8px | 主题切换按钮 | 24px 图标 (Sun/Moon) | 切换 Dark/Light |
| 最右 | 设置入口 | 24px Lucide 图标 (`Settings`) | 跳转 /settings |

**模型切换下拉框交互**:
- 默认态: 显示当前模型名称 + 小箭头图标
- Hover: bg-hover
- 展开: 弹出下拉列表, 每项高 36px, 左侧显示模型提供商 logo (文字占位), 右侧显示模型状态小圆点
- 选中项: 左侧有 `check` 图标

#### 3.3 文件侧边栏 (File Sidebar)

**默认宽度**: 240px, 可拖拽范围 200px-360px, 可完全折叠
**背景**: `bg-surface`
**右侧边框**: 1px `border-default`

**结构 (从上到下)**:

```
┌──────────────────────────┐
│ [搜索框] 🔍 搜索文件...   │  高度 36px, bg-elevated, 圆角 4px
├──────────────────────────┤
│ [+ 新建文档]  按钮        │  文字按钮, accent-primary
├──────────────────────────┤
│ 📁 我的文稿               │  树状结构, Inter 13px
│   ├─ 📄 如何写好小红书.md │    hover: bg-hover (#F5F5F5)
│   ├─ 📄 周报模板.md       │    选中: accent-primary-light + 左侧 3px accent-primary 竖线
│   └─ 📁 小红书文案        │    文件夹可展开/折叠
│       ├─ 📄 第一篇.md     │
│       └─ 📄 第二篇.md     │
├──────────────────────────┤
│ 📁 已发布                 │  分组标题, text-muted, 12px, 大写
│   ├─ 📄 [微信] 春节特辑   │    已发布文件带平台色标签
│   └─ 📄 [小红书] 美食攻略  │
└──────────────────────────┘
```

**交互**:
- 文件项右键菜单: 重命名 / 复制路径 / 删除 (红色)
- 拖拽文件项可移动文件夹
- 搜索框实时过滤, 高亮匹配文字 (accent-primary 底色)

#### 3.4 左栏 — 编辑区 (Editor Panel)

**背景**: `bg-surface`
**内边距**: 顶部 24px, 左右 32px (居中编辑区, 最大宽度 720px 居中)

**编辑器主体**:

| 元素 | 规格 |
|------|------|
| 文档标题输入框 | 无边框, Inter 28px 700, placeholder "输入标题...", text-primary |
| 分割线 | 标题下方 1px `border-subtle`, 上下各 16px 间距 |
| Markdown 编辑区 | JetBrains Mono 14px, 行高 1.7, text-primary, 行号显示在左侧 (text-muted, 24px 宽) |
| 光标 | 蓝绿色竖线 2px (#0D9488), 带 400ms 闪烁动画 |

**编辑器语法高亮配色** (参考 VS Code Dark+):

| 元素 | 颜色 |
|------|------|
| 标题 `#` | `#569CD6` (蓝) |
| 加粗 `**` | `#CE9178` (橙) |
| 链接 `[]()` | `#4EC9B0` (青) |
| 代码 `` | `#D4D4D4` (浅灰) + `#2D2D2D` (背景) |
| 引用 `>` | `#6A9955` (绿) |
| 列表 `- / 1.` | `#D7BA7D` (黄) |

**光标处浮动工具栏** (选中文本时出现):

```
┌───────────────────────────────────────────────┐
│  B  I  ~S~  "  </>  ─  │  /AI  │             │
└───────────────────────────────────────────────┘
```

- **尺寸**: 宽度自适应, 高度 36px, bg-elevated, 圆角 8px, 阴影 `0 4px 16px rgba(0,0,0,0.08)`
- **元素**: 加粗 / 斜体 / 删除线 / 引用 / 代码 / 分割线 / AI 操作入口
- **AI 按钮特殊样式**: 左侧有 1px `accent-ai` 分割线, 点击后展开 AI 子菜单

**Slash Command 菜单** (输入 `/` 触发):

```
┌────────────────────────────────────────┐
│  🔮 AI 操作                            │  分组标题, accent-ai
│  ├─ /大纲  生成文章大纲                │  每项高 40px
│  ├─ /扩写  扩展选定段落                │  左侧 20px 图标区
│  ├─ /润色  小红书风格改写              │  hover: bg-hover
│  ├─ /续写  AI 续写下文                 │  右侧显示快捷键提示
│  └─ /翻译  翻译为英文                  │
│  ──────────────────────                │  分割线
│  📝 插入                               │  分组标题
│  ├─ /图片  插入本地图片                │
│  ├─ /表格  插入表格                    │
│  └─ /代码块  插入代码                  │
└────────────────────────────────────────┘
```

- **尺寸**: 宽度 280px, bg-elevated, 圆角 8px, 阴影 `0 8px 32px rgba(0,0,0,0.1)`
- **定位**: 紧贴光标下方, 左对齐
- **支持键盘上下选择 + Enter 确认 + Esc 关闭**

#### 3.5 左右栏分隔线 (Resizable Divider)

- **宽度**: 1px `border-default`, 两侧各 4px 可交互热区
- **Hover 态**: 显示蓝绿竖线 (accent-primary) + 光标变为 `col-resize`
- **拖拽态**: 蓝绿竖线高亮, 实时更新两侧宽度
- **双击**: 重置为默认 50/50 比例

#### 3.6 右栏 — 预览与分发区 (Preview & Distribution Panel)

**背景**: `bg-surface`
**布局**: 垂直分为三个区域

##### 3.6.1 平台切换 Tab Bar

**位置**: 右栏顶部
**高度**: 44px
**背景**: `bg-surface`, 底部 1px `border-default`

```
┌──────────────────────────────────────────────┐
│  [微信公众号预览]   [小红书预览]   │  AI检查  │
└──────────────────────────────────────────────┘
```

| Tab | 图标 | 激活色 | 说明 |
|-----|------|--------|------|
| 微信公众号预览 | Lucide `MessageCircle` | `accent-wechat` (#07C160) 下划线 2px | 默认选中 |
| 小红书预览 | Lucide `BookOpen` | `accent-xhs` (#FF2442) 下划线 2px | 切换为小红书卡片样式 |
| AI 违禁词检查 | Lucide `ShieldCheck` | `accent-ai` (#8B5CF6) | 右侧独立按钮, 点击触发 AI 校验 |

**Tab 交互**:
- 未选中: text-secondary, 无下划线
- Hover: text-primary
- 选中: text-primary + 底部 2px 实线 + 对应平台色

##### 3.6.2 预览渲染区

**位置**: Tab Bar 下方, 占据右栏主要空间
**内边距**: 24px
**背景**: 可选白底 (模拟真实平台) 或 `bg-surface`

**微信公众号预览模式**:

```
┌─────────────────────────────────────┐
│  模拟手机屏幕外框                    │
│  ┌─────────────────────────────┐    │
│  │  微信公众号样式渲染区         │    │
│  │                             │    │
│  │  标题: 22px 微软雅黑 加粗    │    │  max-width: 375px
│  │  正文: 17px 系统字体         │    │  居中显示
│  │  图片: 自适应宽度            │    │  背景: #FFFFFF
│  │  引用: 左边框 + 灰底         │    │
│  │  代码: 灰底等宽字体          │    │
│  │                             │    │
│  └─────────────────────────────┘    │
└─────────────────────────────────────┘
```

- 外框: 无, 直接渲染 HTML
- 内容区: 最大宽度 375px 居中, 背景白色, 模拟公众号文章样式
- 字体: 系统默认 (微软雅黑优先), 标题 22px 加粗, 正文 17px, 行高 1.75
- 图片: 自适应宽度, 圆角 4px
- 整体外层有手机比例的容器, 顶部可显示 "微信公众号" 标识

**小红书预览模式**:

```
┌─────────────────────────────────────┐
│  ┌─────────────────────────────┐    │
│  │  📷 封面图区域               │    │
│  │  (首图 / 拖拽上传区)         │    │
│  │  比例: 3:4                   │    │
│  ├─────────────────────────────┤    │
│  │  正文标题 (加粗)             │    │
│  │  正文内容...                 │    │
│  │  #标签1 #标签2               │    │  卡片样式
│  ├─────────────────────────────┤    │  max-width: 375px
│  │  ❤️ 0  💬 0  ⭐ 0          │    │
│  └─────────────────────────────┘    │
└─────────────────────────────────────┘
```

- 卡片式布局, 圆角 12px, bg-surface
- 顶部: 3:4 比例图片区, 可显示封面图或拖拽上传占位
- 正文: 标题 16px 加粗, 内容 14px, 带小红书风格排版 (emoji、短段落)
- 底部: 互动数据占位 (纯展示)
- 标签: `#标签` 以 accent-xhs 高亮

##### 3.6.3 AI 违禁词检查面板

点击 "AI 检查" 后, 预览区上方插入检查结果条:

```
┌──────────────────────────────────────────────┐
│  🛡️ AI 内容检查结果                           │
│  ⚠️ 发现 2 处潜在违禁词:                      │
│     第12行: "最好的" → 建议改为 "优秀的"       │
│     第28行: "绝对有效" → 建议改为 "可能有效"   │
│  [全部替换]  [忽略]                           │
└──────────────────────────────────────────────┘
```

- 背景: `#FFFBEB` (警告浅底), 左边框 3px `status-warning`
- 问题行在编辑器中高亮 (黄色波浪下划线)

##### 3.6.4 分发控制台 (Distribution Console)

**位置**: 右栏底部固定
**高度**: 折叠态 44px, 展开态 240px (可拖拽调高)
**背景**: `#F9FAFB` (浅灰底), 日志区 `#1A1A1A` (深色终端区)

```
┌──────────────────────────────────────────────┐
│  ▶ 分发控制台                    [▲ 展开]     │  44px 头部, bg-surface
├──────────────────────────────────────────────┤
│  [一键发布到微信公众号]  [一键发布到小红书]    │  按钮区
│                                              │
│  ┌──────────────────────────────────────────┐│
│  │ > [12:01:03] OK 正在连接 Chrome...       ││  日志输出窗
│  │ > [12:01:05] OK 正在上传图片 (1/3)...    ││  JetBrains Mono 12px
│  │ > [12:01:08] OK 正在填写正文...          ││  行高 1.6
│  │ > [12:01:12] !! 错误: 图片上传失败       ││  滚动显示
│  │ > [12:01:15] .. 正在重试...              ││  颜色: 绿/红/黄/蓝
│  │ > [12:01:18] OK 发布成功!                ││
│  └──────────────────────────────────────────┘│
└──────────────────────────────────────────────┘
```

**发布按钮规格**:

| 按钮 | 样式 | 状态 |
|------|------|------|
| 一键发布到微信公众号 | bg: `accent-wechat`, 文字白色, 圆角 6px, 高 36px | 默认/hover/执行中(禁用+spinner)/完成(check) |
| 一键发布到小红书 | bg: `accent-xhs`, 文字白色, 圆角 6px, 高 36px | 同上 |

**按钮状态流转**:
- **默认态**: 平台色背景, 白色文字
- **Hover**: 亮度提升 10%, cursor-pointer
- **执行中**: 禁用态, 背景色降低饱和度, 左侧显示旋转 spinner, 文字变为 "发布中..."
- **成功**: 背景变为 `status-success`, 文字 "发布成功", 2 秒后恢复默认
- **失败**: 背景变为 `status-error`, 文字 "发布失败, 点击重试", 点击可重试

**日志输出窗**:
- 背景: `#1A1A1A` (深色终端区, 与亮色主题形成对比)
- 字体: JetBrains Mono 12px, 文字 `#D1D5DB`
- 行高: 1.6
- 最大显示 200 行, 超出自动滚动到底部
- 日志时间戳: `#6B7280`
- 日志级别颜色: `OK` 成功 = status-success, `!!` 错误 = status-error, `..` 警告 = status-warning, `--` 信息 = status-info
- 顶部渐隐遮罩: 从透明到 `#1A1A1A`, 高 16px

---

### P2: 移动端编辑页 (Mobile — 375px)

#### 3.7 整体布局

```
┌──────────────────────────┐
│  [TOP BAR] 48px          │
├──────────────────────────┤
│                          │
│  [FULL-SCREEN EDITOR]    │
│                          │
│  全屏 Markdown 编辑器     │
│                          │
│                          │
├──────────────────────────┤
│  [BOTTOM TOOLBAR] 52px   │
├──────────────────────────┤
│     [FAB BUTTON]         │
│     56px 圆形, 上移 -28px │
└──────────────────────────┘
```

#### 3.8 移动端顶栏

**高度**: 48px, bg-surface
**左侧**: 返回箭头 (`ArrowLeft`, 24px) + 文件名 (14px, 单行截断)
**右侧**: 保存状态指示 ("已保存" / 旋转中), 13px text-secondary

#### 3.9 移动端编辑器

- **内边距**: 顶部 16px, 左右 20px
- **字体**: 系统等宽字体, 16px (避免 iOS 自动缩放), 行高 1.7
- **标题**: 同 PC 端样式
- **无行号显示** (节省空间)
- **键盘弹出时**: 编辑区自动滚动到光标位置, 顶栏保持固定

#### 3.10 移动端底部工具栏

**高度**: 52px, bg-surface, 顶部 1px border-default
**Safe Area**: 底部 padding 适配 iPhone 底部安全区

工具栏图标按钮 (水平排列, 均分宽度):

| 图标 | 功能 | 大小 |
|------|------|------|
| `Type` | 格式化 (加粗/斜体/标题) | 24px |
| `List` | 列表 | 24px |
| `Code` | 代码块 | 24px |
| `Image` | 插入图片 | 24px |
| `Undo` | 撤销 | 24px |

#### 3.11 FAB 悬浮操作按钮

**位置**: 底部居中, 向上偏移 28px (悬浮在底部工具栏上方)
**尺寸**: 56px 圆形
**背景**: `accent-primary` (#0D9488), 纯色无渐变
**图标**: Lucide `Sparkles` 24px, 白色
**阴影**: `0 4px 16px rgba(13,148,136,0.3)`

**点击后弹出 Bottom Sheet**:

```
┌──────────────────────────┐
│         ━━━ (拖拽条)      │  顶部 40px, 居中 32x4 灰色条
│                          │
│  🔮 AI 操作              │  分组标题
│  ├─ 生成大纲             │  列表项 48px
│  ├─ 扩写段落             │
│  ├─ 小红书润色           │
│  ├─ AI 续写              │
│  └─ 翻译为英文           │
│  ──────────────          │
│  📱 多端预览             │  点击跳转 /preview
│  ──────────────          │
│  🚀 执行发布             │  展开发布子选项
│  ├─ 发布到微信公众号      │
│  └─ 发布到小红书          │
└──────────────────────────┘
```

**Bottom Sheet 规格**:
- 圆角: 顶部 16px
- 背景: `bg-surface`
- 最大高度: 屏幕 70%
- 列表项: 48px 高, hover 态 bg-hover, 左侧 16px 图标 + 16px 文字
- 底部 safe area padding
- 遮罩: `rgba(0,0,0,0.5)`, 点击遮罩关闭
- 手势: 下滑关闭

---

### P3: 移动端预览页 (`/preview` Mobile)

#### 3.12 页面结构

```
┌──────────────────────────┐
│  [TOP BAR]               │
│  ← 返回  │ 预览          │
├──────────────────────────┤
│  [平台切换 Segmented]     │
│  [微信公众号] [小红书]    │
├──────────────────────────┤
│                          │
│  [预览内容区]             │
│  (同 PC 预览样式,         │
│   但自适应全屏宽度)       │
│                          │
│                          │
└──────────────────────────┘
```

**平台切换**: iOS 风格 Segmented Control, 高 32px, 圆角 8px, bg-elevated
**预览区**: 全宽, 无居中限制, 内边距 16px

---

### P4: 设置页 (`/settings`)

#### 3.13 PC 端设置页

**布局**: 左侧导航 + 右侧内容区, 最大宽度 960px 居中

**左侧导航** (宽度 200px):

| 项目 | 图标 | 说明 |
|------|------|------|
| AI 模型配置 | `Brain` | API Key 管理 |
| 发布账号 | `Users` | 微信/小红书账号 |
| 编辑器偏好 | `Sliders` | 字体/主题/快捷键 |
| 数据管理 | `Database` | 存储路径/导出/备份 |
| 关于 | `Info` | 版本/更新日志 |

**右侧内容 — AI 模型配置示例**:

```
┌──────────────────────────────────────────────┐
│  AI 模型配置                                  │  Heading 1
│                                              │
│  DeepSeek                                    │  Heading 2
│  ┌──────────────────────────────────────────┐│
│  │  API Key                                 ││  Label
│  │  ┌──────────────────────────────────┐    ││
│  │  │  sk-xxxx...xxxx  [显示/隐藏]     │    ││  密码输入框
│  │  └──────────────────────────────────┘    ││
│  │  模型选择                                ││
│  │  ┌──────────────────────────────────┐    ││
│  │  │  deepseek-chat (默认)      ▼     │    ││  下拉选择
│  │  └──────────────────────────────────┘    ││
│  │  [测试连接]                              ││  次要按钮
│  └──────────────────────────────────────────┘│
│                                              │
│  Claude                                      │  Heading 2
│  ┌──────────────────────────────────────────┐│
│  │  (同上结构)                               ││
│  └──────────────────────────────────────────┘│
│                                              │
│  通义千问                                     │  Heading 2
│  ┌──────────────────────────────────────────┐│
│  │  (同上结构)                               ││
│  └──────────────────────────────────────────┘│
└──────────────────────────────────────────────┘
```

**输入框规格**:
- 高度: 40px, bg-elevated, 圆角 6px, 边框 1px `border-default`
- Focus 态: 边框变为 `accent-primary`, 显示 2px ring
- 密码输入框右侧: 眼睛图标切换显隐
- "测试连接" 按钮: 次要样式 (bg-transparent, border accent-primary, 文字 accent-primary)

#### 3.14 移动端设置页

- 无左侧导航, 改为顶部 Tab 或列表式导航
- 全宽布局, 无最大宽度限制
- 分组以卡片形式呈现, 间距 12px

---

### P5: 文件管理侧边栏 (内嵌组件, 已在 3.3 详述)

---

## 四、核心组件库

### 4.1 按钮 (Button)

| 类型 | 背景 | 文字 | 边框 | 高度 | 圆角 | 用途 |
|------|------|------|------|------|------|------|
| Primary | `accent-primary` (#0D9488) | 白色 | 无 | 36px | 8px | 主操作 (保存/确认) |
| CTA | `accent-cta` (#EA580C) | 白色 | 无 | 36px | 8px | 核心发布入口 |
| Secondary | 透明 | `accent-primary` | 1px `accent-primary` | 36px | 8px | 次操作 (取消/测试) |
| Ghost | 透明 | `text-secondary` | 无 | 36px | 8px | 工具栏操作 |
| Danger | `status-error` | 白色 | 无 | 36px | 8px | 删除/危险操作 |
| Wechat | `accent-wechat` | 白色 | 无 | 36px | 8px | 微信发布 |
| XHS | `accent-xhs` | 白色 | 无 | 36px | 8px | 小红书发布 |

**通用交互状态**:
- Hover: 亮度提升 10%
- Active/Pressed: 亮度降低 5%, scale 0.98
- Disabled: opacity 0.4, cursor not-allowed
- Loading: 左侧 spinner 16px + 文字

### 4.2 输入框 (Input)

- **默认态**: bg-elevated (#FFFFFF), border 1px border-default (#E5E5E5), 高 40px, 圆角 6px, 内边距 12px
- **Focus 态**: border accent-primary (#0D9488), ring 0 0 0 3px rgba(13,148,136,0.12)
- **Error 态**: border status-error, 底部显示错误文字 12px status-error
- **Disabled 态**: opacity 0.5, bg-surface

### 4.3 下拉选择 (Select/Dropdown)

- 同输入框样式 + 右侧 ChevronDown 图标
- 下拉面板: bg-elevated, 圆角 8px, 阴影, 选项高 36px
- 选中项: 左侧 Check 图标 + bg-hover

### 4.4 标签 (Tag/Badge)

| 类型 | 背景 | 文字 | 圆角 | 用途 |
|------|------|------|------|------|
| 平台-微信 | rgba(7,193,96,0.15) | accent-wechat | 4px | 微信相关标签 |
| 平台-小红书 | rgba(255,36,66,0.15) | accent-xhs | 4px | 小红书相关标签 |
| AI | rgba(139,92,246,0.15) | accent-ai | 4px | AI 功能标签 |
| 状态 | rgba(34,197,94,0.15) | status-success | 4px | 已发布/成功 |
| 状态 | rgba(239,68,68,0.15) | status-error | 4px | 发布失败 |

### 4.5 Toast 通知

- **位置**: 右上角 (PC) / 顶部 (Mobile)
- **尺寸**: 宽 360px, 高 auto, bg-elevated, 圆角 8px
- **左侧**: 20px 状态图标 (成功/错误/警告/信息)
- **右侧**: 标题 14px 600 + 描述 13px 400 text-secondary
- **关闭**: 右侧 X 图标
- **自动消失**: 3 秒后淡出 (200ms ease-out)

### 4.6 Modal 弹窗

- **遮罩**: rgba(0,0,0,0.5)
- **弹窗**: bg-surface, 圆角 12px, 最大宽度 480px, 居中
- **头部**: 48px, 标题左对齐, 右上角 X 关闭
- **内容区**: 内边距 24px
- **底部**: 操作按钮右对齐 (取消 + 确认), 间距 12px
- **动画**: scale 0.95→1 + opacity 0→1, 200ms ease-out

### 4.7 日志输出窗 (Terminal Log)

- **背景**: `#1A1A1A` (深色终端区, 与亮色主题形成层次对比)
- **字体**: JetBrains Mono 12px, 行高 1.6, 文字 `#D1D5DB`
- **行号**: 可选, `#4B5563`
- **自动滚动**: 新日志出现时自动滚动到底部
- **时间戳**: 格式 `[HH:mm:ss]`, 颜色 `#6B7280`
- **交互**: 支持选中复制, 不可编辑

---

## 五、动效与过渡规范

| 场景 | 属性 | 时长 | 缓动 |
|------|------|------|------|
| 侧边栏展开/收起 | width | 200ms | ease-in-out |
| Tab 切换 | 内容淡入淡出 | 150ms | ease |
| Bottom Sheet 弹出 | transform translateY | 300ms | cubic-bezier(0.32,0.72,0,1) |
| Bottom Sheet 关闭 | transform translateY | 200ms | ease-in |
| 按钮 hover | background-color | 150ms | ease |
| Toast 出现 | opacity + translateY(-8px) | 200ms | ease-out |
| Toast 消失 | opacity | 200ms | ease-in |
| 日志新行 | 无动画, 即时出现 | 0ms | - |
| 拖拽分隔线 | 实时跟随 | 0ms | - |
| AI 结果流式输出 | 逐字显示 | 每字 20ms | - |
| 文件树展开/折叠 | height + opacity | 150ms | ease |
| 模态框出现 | scale(0.95→1) + opacity | 200ms | ease-out |
| 页面路由切换 | opacity | 150ms | ease |

---

## 六、响应式断点

| 断点 | 宽度 | 布局 |
|------|------|------|
| Mobile | < 768px | 单栏 + 底部 Tab + FAB |
| Tablet | 768px - 1023px | 单栏 + 可切换编辑/预览模式 |
| Desktop | >= 1024px | 双栏布局 |

**Tablet 中间态处理**:
- 默认显示编辑区, 右栏折叠为浮动预览按钮
- 点击预览按钮: 右侧以 overlay 形式滑出预览面板 (宽度 50%, 可关闭)
- 文件侧边栏: 默认收起, 点击顶栏按钮展开为 overlay

---

## 七、AI 设计工具输入指南

### 使用 v0.dev 生成

建议分模块生成, 按以下顺序:

1. **设计 Token 定义**: 将色彩系统和字体系统输入为 CSS 变量
2. **PC 主工作台**: 描述整体布局结构 + 各区域内容
3. **编辑器组件**: Markdown 编辑器 + 浮动工具栏 + Slash Command
4. **预览组件**: 微信公众号预览 + 小红书卡片预览
5. **分发控制台**: 按钮 + 日志窗
6. **移动端**: 编辑页 + Bottom Sheet + 预览页
7. **设置页**: 表单布局 + 输入框组件

### 使用 Figma AI 生成

建议按照 Frame 结构:
- Frame 1: PC 1440x900 — 主工作台全貌
- Frame 2: PC 1440x900 — Slash Command 展开态
- Frame 3: PC 1440x900 — AI 文本选中工具栏
- Frame 4: PC 1440x900 — 小红书预览模式
- Frame 5: PC 1440x900 — 发布执行中状态
- Frame 6: Mobile 375x812 — 编辑页
- Frame 7: Mobile 375x812 — Bottom Sheet 展开
- Frame 8: Mobile 375x812 — 预览页
- Frame 9: PC 1440x900 — 设置页

### 关键提示词 (Prompt Keywords)

```
Light mode modern warm UI, Notion meets Feishu aesthetic,
flat design with no shadows, solid color blocks only,
Plus Jakarta Sans for UI text, JetBrains Mono for code,
clean white background #FAFAFA with white surface cards,
teal primary color #0D9488, warm orange CTA #EA580C,
split-panel layout with resizable divider,
terminal-style log console at bottom (dark bg for contrast),
platform-colored action buttons (green for WeChat, red for Xiaohongshu, purple for AI),
Markdown editor with syntax highlighting,
mobile-first responsive with bottom sheet navigation,
consistent 4px/8px spacing grid, border-radius 8px,
friendly and approachable, zero decoration, no emoji icons — use Lucide line icons only
```