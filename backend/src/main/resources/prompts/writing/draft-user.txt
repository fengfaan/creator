{context}
请基于上方大纲生成完整 Markdown 正文。
要求：
- 必须覆盖大纲中的每一个一级/二级条目，不要只写前半部分。
- 每个主要小节至少扩写 2-4 段。
- 如果篇幅较长，优先保证所有章节都有完整内容，而不是在前几节写得过长。
- 直接输出正文，不要重复输出大纲。
- 必须保留原文中所有图片标记（`![alt](url)` 语法及紧跟的 `> caption` 引用块），将它们保留在原位或放在对应的段落附近。{instruction}